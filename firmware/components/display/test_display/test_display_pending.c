/**
 * @file test_display_pending.c
 * @brief Placeholder Unity test file for the display component.
 *
 * Display unit tests will be added in Phase 1.5 (page scheduler, label
 * mapping, number formatting, SIM badge logic). For now this file exists
 * only to give the display component a valid test/ subdirectory so the
 * Unity test runner has something to register.
 */
#include "unity.h"

TEST_CASE("display: placeholder until Phase 1.5", "[display][pending]")
{
    TEST_IGNORE_MESSAGE("Phase 1.5 will populate these tests.");
}
