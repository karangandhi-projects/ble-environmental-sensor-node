# Display component skeleton — populated in Phase 1.5 with SSD1306 driver,
# big bitmap font, and page-rotation scheduler. For now this is a
# header-only/interface component with no sources.
idf_component_register(
    INCLUDE_DIRS "include"
)
