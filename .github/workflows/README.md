# CI Placeholder

A future CI workflow can build the ESP-IDF project in a Docker image or Espressif GitHub Action.

Do not enable CI until the local ESP-IDF version is chosen and the project compiles locally.
