# Tests

This folder contains validation material for the BLE Environmental Sensor Node.

MVP testing is manual because BLE behavior must be verified with a real central such as nRF Connect.

Future automated tests can be added for:
- Telemetry frame encoding.
- Config validation.
- Control opcode parsing.
- Sensor provider simulation.
